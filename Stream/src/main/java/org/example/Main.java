package org.example;

import org.example.stream.Limit;
import org.example.stream.Reduce;
import org.example.stream.Skip;

public class Main {
    public static void main(String[] args) {

        Limit limit=new Limit();
        limit.limit();

    }
}